import React from 'react';
import { View, Text } from 'react-native';

export default function AdvisoryScreen({navigation}) {
    return (
        <View>
            <Text>Advise for crops</Text>
        </View>
    )
}
